/*
  consolidate all env vars here as an object
  as if they are read from config.yaml as they were in v1
*/

// for loading env vars
import { load } from "./deps.ts";

//console.log( "env-vars.ts : env : ", env );

